from googletrans import Translator
t = Translator()
a = input("Enter text you want to convert: ")
b = input("Enter target language (e.g., 'hi' for Hindi, 'fr' for French): ")
res = t.translate(a, dest=b)
print(res.text)
